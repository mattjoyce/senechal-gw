pipelines:
  - name: if-demo
    on: demo.start
    execution_mode: synchronous
    timeout: 10s
    steps:
      - id: guarded
        uses: sys_exec
        if:
          path: payload.run_guarded
          op: eq
          value: true
      - id: final
        uses: sys_exec
