{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mattjoyce/ductile/schemas/plugins.schema.json",
  "title": "Ductile Plugins File",
  "description": "Schema for ductile plugins.yaml. In multi-file mode this is loaded via include and deep-merged, so it may contain only 'plugins' or any other mergeable top-level keys.",
  "type": "object",
  "properties": {
    "plugins": {
      "type": "object",
      "description": "Per-plugin configuration keyed by plugin name.",
      "additionalProperties": {
        "$ref": "config.schema.json#/$defs/PluginConf"
      }
    },
    "include": {
      "type": "array",
      "description": "Further files to merge.",
      "items": { "type": "string" }
    }
  },
  "required": ["plugins"],
  "additionalProperties": false
}
