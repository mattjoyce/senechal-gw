{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mattjoyce/ductile/schemas/plugins.schema.json",
  "title": "Ductile Plugins",
  "description": "Schema for ductile plugins.yaml — standalone plugin configuration file (multi-file mode).",
  "type": "object",
  "properties": {
    "plugins": {
      "type": "object",
      "description": "Per-plugin configuration keyed by plugin name.",
      "additionalProperties": {
        "$ref": "config.schema.json#/$defs/PluginConf"
      }
    }
  },
  "required": ["plugins"],
  "additionalProperties": false
}
