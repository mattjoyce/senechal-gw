pipelines:
  - name: if-sync
    on: demo.start
    execution_mode: synchronous
    timeout: 5s
    steps:
      - id: first
        uses: step-a
        if:
          path: payload.run_first
          op: eq
          value: true
      - id: second
        uses: step-b
