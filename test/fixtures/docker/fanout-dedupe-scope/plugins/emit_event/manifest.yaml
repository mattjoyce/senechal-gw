manifest_spec: ductile.plugin
manifest_version: 1
name: emit_event
version: 0.1.0
protocol: 2
entrypoint: run.sh
description: "Fixture plugin: emits a configurable event with a configurable payload."
commands:
  - name: handle
    type: write
    description: "Emit configured event with merged payload."
    values:
      consume: []
      emit: []
