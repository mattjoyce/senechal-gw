plugins:
  plugin_a:
    uses: echo
    enabled: true
    config:
      message: "plugin_a"
    notify_on_complete: true

  plugin_b:
    uses: echo
    enabled: true
    config:
      message: "plugin_b"
    notify_on_complete: true

  hook_scoped_to_a:
    uses: echo
    enabled: true
    config:
      message: "hook_scoped_to_a fired"

  hook_scoped_to_b:
    uses: echo
    enabled: true
    config:
      message: "hook_scoped_to_b fired"

  hook_always:
    uses: echo
    enabled: true
    config:
      message: "hook_always fired"
