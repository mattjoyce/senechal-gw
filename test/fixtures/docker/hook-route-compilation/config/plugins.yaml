plugins:
  root_echo:
    uses: echo
    enabled: true
    notify_on_complete: true
    config:
      message: "hook route source"

  hook_echo:
    uses: echo
    enabled: true
    config:
      message: "hook route sink"
