plugins:
  file_watch:
    enabled: true
    config:
      watches:
        - id: "single-file"
          path: "/tmp/file-to-watch.txt"
          event_type: "file.changed"
          emit_initial: true
    schedules:
      - command: poll
        every: 1s
  echo:
    enabled: true
