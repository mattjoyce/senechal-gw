manifest_spec: ductile.plugin
manifest_version: 1
name: emit_event_with_role
version: 0.1.0
protocol: 2
entrypoint: run.sh
description: "Fixture plugin: emits an event with role in payload for baggage."
commands:
  - name: handle
    type: write
    description: "Emit event with role payload."
    values:
      consume: []
      emit: []
