name: echo
version: 0.1.0
protocol: 2
entrypoint: run.sh
description: "Reads protocol v2 request on stdin; returns logs + state_updates with last_run timestamp"
commands:
  - name: poll
    type: write
  - name: health
    type: read
config_keys:
  required: []
  optional: [message, mode]
