name: echo
version: 0.1.0
protocol: 1
entrypoint: run.sh
description: "Reads protocol v1 request on stdin; returns logs + state_updates with last_run timestamp"
commands:
  - name: poll
    type: write
  - name: health
    type: read
config_keys:
  required: []
  optional: [message, mode]
